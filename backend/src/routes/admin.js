import {Router} from 'express';import Booking from '../models/Booking.js';import Property from '../models/Property.js';import User from '../models/User.js';import {auth,admin} from '../middleware.js';
const r=Router();r.use(auth,admin);r.get('/stats',async(req,res)=>{const [properties,users,confirmed,cancelled,revenue]=await Promise.all([Property.countDocuments(),User.countDocuments(),Booking.countDocuments({status:'confirmed'}),Booking.countDocuments({status:'cancelled'}),Booking.aggregate([{$match:{paymentStatus:'paid'}},{$group:{_id:null,total:{$sum:'$totalAmount'}}}])]);res.json({properties,users,confirmed,cancelled,revenue:revenue[0]?.total||0})});export default r;
