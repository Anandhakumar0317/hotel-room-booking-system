import 'dotenv/config';import express from 'express';import cors from 'cors';import morgan from 'morgan';import mongoose from 'mongoose';import properties from './routes/properties.js';import bookings from './routes/bookings.js';import users from './routes/users.js';import authRoutes from './routes/auth.js';import adminRoutes from './routes/admin.js';
const app=express();app.use(cors({origin:process.env.FRONTEND_URL||true}));app.use(express.json({limit:'2mb'}));app.use(morgan('dev'));app.get('/api/health',(req,res)=>res.json({ok:true,version:'3.0.0'}));app.use('/api/properties',properties);app.use('/api/bookings',bookings);app.use('/api/users',users);app.use('/api/auth',authRoutes);app.use('/api/admin',adminRoutes);const port=process.env.PORT||5000;mongoose.connect(process.env.MONGO_URI||'mongodb://127.0.0.1:27017/hotel_booking').then(()=>app.listen(port,()=>console.log(`StayEasy API http://localhost:${port}`))).catch(e=>{console.error(e);process.exit(1)});
