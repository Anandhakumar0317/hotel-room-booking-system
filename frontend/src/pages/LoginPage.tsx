import {useState} from 'react';import {useDispatch} from 'react-redux';import {useNavigate} from 'react-router-dom';import {Paper,TextField,Button,Typography,Stack,Alert} from '@mui/material';import {api} from '../api';import {login} from '../store/store';export default function LoginPage(){const d=useDispatch();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const go=async()=>{try{const x=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});d(login(x));nav(x.user.role==='admin'?'/admin':'/')}catch(e:any){setErr(e.message)}};return <Paper sx={{p:4,maxWidth:450,mx:'auto'}}><Typography variant="h4" fontWeight={800}>Login</Typography><Stack spacing={2} sx={{mt:2}}><TextField label="Email" value={email} onChange={e=>setEmail(e.target.value)}/><TextField label="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<Alert severity="error">{err}</Alert>}<Button variant="contained" onClick={go}>Login</Button></Stack></Paper>}
